<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/linearLayout"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="top|right"
    android:orientation="vertical"
    android:padding="5px"
    app:cardBackgroundColor="#00FFFFFF"
    app:cardCornerRadius="10dp">


    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <Button
            android:id="@+id/editprof_btn"
            style="@style/Widget.AppCompat.Button.Borderless"
            android:layout_width="175dp"
            android:layout_height="45dp"
            android:layout_marginTop="8dp"
            android:background="#FFFFFF"
            android:onClick="EditProfile"
            android:text="Edit Profile"
            android:textAllCaps="false"
            android:textColor="@color/text_gray"
            app:backgroundTint="#FFFFFF"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.502"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/changepass_btn" />

        <Button
            android:id="@+id/logout_btn"
            style="@style/Widget.AppCompat.Button.Borderless"
            android:layout_width="175dp"
            android:layout_height="45dp"
            android:layout_marginTop="8dp"
            android:background="#FFFFFF"
            android:onClick="LogOutUser"
            android:text="Log Out"
            android:textAllCaps="false"
            android:textColor="@color/text_gray"
            app:backgroundTint="#FFFFFF"
            app:layout_constraintEnd_toEndOf="@+id/update_pass_btn"
            app:layout_constraintHorizontal_bias="0.0"
            app:layout_constraintStart_toStartOf="@+id/editprof_btn"
            app:layout_constraintTop_toBottomOf="@+id/editprof_btn" />

        <Button
            android:id="@+id/changepass_btn"
            style="@style/Widget.AppCompat.Button.Borderless"
            android:layout_width="175dp"
            android:layout_height="45dp"
            android:background="#FFFFFF"
            android:onClick="ChangePassword"
            android:text="Change Password"
            android:textAllCaps="false"
            android:textColor="@color/text_gray"
            app:backgroundTint="#FFFFFF"
            app:layout_constraintEnd_toEndOf="@+id/update_pass_btn"
            app:layout_constraintHorizontal_bias="0.0"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>